#include<iostream>
#include "rec.h"
using namespace std ;

int main() {
    // rectangle r1;
    // r1.set_length(5);
    // r1.set_width(5);
    // cout<<r1.get_area();

    Rectangle r1 (10,5);  //parametrized constructor with destructor

    return 0;
}